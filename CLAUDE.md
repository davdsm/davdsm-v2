## Design Context

Before making UI changes, read `PRODUCT.md` (register, users, positioning, brand personality, anti-references) and `DESIGN.md` (color/type tokens, elevation, components, do's and don'ts). Both are maintained by the `impeccable` skill — re-run `/impeccable document` if `DESIGN.md` drifts from the code.
